import React from 'react'
import Flexbox from 'flexbox-react'
export default props => (
  <Flexbox flexGrow={1} style={{border: '1px solid red'}}>
    <span style={{fontSize: 24}}>home</span>
  </Flexbox>
)
